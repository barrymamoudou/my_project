{% extends 'base.html.twig' %}

{% block title 'Gère les Biens' %}Edition{% endblock %}

{% block body %}

<div class="contenair mt-4">
    <h1>Gère les bien</h1>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for property in properties %}
             <tr>
                <td>{{property.title}}</td>
                <td>
                    <a href="{{ path('')}}" class=""></a>
                </td>
             </tr>

            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
